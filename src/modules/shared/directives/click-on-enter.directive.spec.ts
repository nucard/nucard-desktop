import { ClickOnEnterDirective } from './click-on-enter.directive';

describe('ClickOnEnterDirective', () => {
    it('should create an instance', () => {
        const directive = new ClickOnEnterDirective();
        expect(directive).toBeTruthy();
    });
});
