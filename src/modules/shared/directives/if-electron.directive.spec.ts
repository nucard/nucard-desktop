import { IfElectronDirective } from './if-electron.directive';

describe('IfElectronDirective', () => {
  it('should create an instance', () => {
    const directive = new IfElectronDirective();
    expect(directive).toBeTruthy();
  });
});
