import { RulesSymbolsPipe } from './rules-symbols.pipe';

describe('RulesSymbolsPipe', () => {
  it('create an instance', () => {
    const pipe = new RulesSymbolsPipe();
    expect(pipe).toBeTruthy();
  });
});
